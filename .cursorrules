# 简历项目开发规则

## 📝 更新日志规则

### 强制要求
每次修改代码后，**必须**在 `UPDATES_PAGE_SUMMARY.md` 文件中记录：

1. **更新时间**：使用格式 `YYYY-MM-DD HH:mm`
2. **更新内容**：简要描述修改内容
3. **修改文件路径**：列出所有修改的文件完整路径
4. **修改原因**：说明为什么要做这个修改
5. **影响范围**：说明修改影响的功能模块

### 更新日志格式模板
```markdown
## [YYYY-MM-DD HH:mm] 更新标题

### 修改内容
- 具体修改点1
- 具体修改点2

### 修改文件
- `/path/to/file1.tsx`
- `/path/to/file2.css`

### 修改原因
说明为什么要做这个修改

### 影响范围
- 影响的功能模块1
- 影响的功能模块2
```

---

## 🎨 设计规范（基于 frontend-design skill）

### 字体系统
- **标题字体**：`Crimson Pro` (衬线，优雅)
- **正文字体**：`DM Sans` (无衬线，现代)
- **代码字体**：`JetBrains Mono` (等宽)
- **禁止使用**：Inter, Roboto, Arial, 系统默认字体

### 色彩系统 - Editorial Modern
- **主色调**：深墨色 `#1c1917` (stone-900)
- **强调色**：琥珀金 `#d97706` (amber-600)
- **辅助色**：石板灰 `#64748b` (slate-500)
- **背景色**：纸白色 `#fafaf9` (stone-50)
- **禁止使用**：蓝紫渐变、通用蓝色、粉色渐变

### 视觉风格
- **美学方向**：Editorial Modern（编辑美学）
- **布局风格**：不对称、杂志式排版
- **动效原则**：精致、克制、有目的性
- **圆角规范**：使用 `rounded-lg` (8px)，避免 `rounded-full`
- **边框规范**：使用 2px 实线边框，避免细边框

---

## 💻 代码规范

### 版权信息
每个新建文件必须包含版权信息：
```typescript
/**
 * @copyright Tomda (https://www.tomda.top)
 * @copyright UIED技术团队 (https://fsuied.com)
 * @author UIED技术团队
 * @createDate YYYY.MM.DD
 */
```

### 注释规范
- 所有函数必须有中文注释
- 复杂逻辑必须添加行内注释
- 组件必须有功能说明注释

### 命名规范
- 组件：PascalCase (例：`ResumeEditor`)
- 函数：camelCase (例：`handleSubmit`)
- 常量：UPPER_SNAKE_CASE (例：`MAX_FILE_SIZE`)
- CSS类：kebab-case (例：`resume-preview`)

### 文件组织
```
src/
├── app/              # Next.js 页面
├── components/       # React 组件
├── hooks/           # 自定义 Hooks
├── utils/           # 工具函数
├── types/           # TypeScript 类型
├── styles/          # 全局样式
└── contexts/        # React Context
```

---

## 🚀 性能优化规范

### 图片优化
- 使用 Next.js `Image` 组件
- 懒加载非关键图片
- 使用 WebP 格式

### 代码分割
- 动态导入大型组件
- 使用 `dynamic` 导入非首屏组件
- 避免在首页加载过多依赖

### 动画优化
- 优先使用 CSS 动画
- 避免过度使用 Framer Motion
- 减少不必要的动画效果

---

## 🧪 测试规范

### 必须测试的场景
- 导出功能（PDF、PNG）
- 模板切换
- 响应式布局
- AI 功能

### 测试文件位置
- 组件测试：`src/components/__tests__/`
- 工具函数测试：`src/utils/__tests__/`

---

## 📦 依赖管理

### 安装新依赖前必须
1. 检查是否有现有依赖可以实现
2. 评估包的大小和维护状态
3. 记录在 `package.json` 中

### 禁止的依赖
- 避免安装过大的 UI 库
- 避免重复功能的包

---

## 🔒 安全规范

### API 密钥管理
- 所有密钥存储在 `.env.local`
- 不提交 `.env.local` 到 Git
- 使用环境变量访问密钥

### 用户数据
- 简历数据仅存储在本地
- 不上传用户隐私信息
- 导出功能在客户端完成

---

## 📱 响应式设计

### 断点规范
- Mobile: < 640px
- Tablet: 640px - 1024px
- Desktop: > 1024px

### 移动端优先
- 使用 Tailwind 的响应式前缀
- 测试所有断点的显示效果

---

## 🐛 错误处理

### 必须处理的错误
- 文件上传失败
- 导出失败
- API 调用失败
- 网络错误

### 错误提示
- 使用 Toast 组件显示错误
- 提供明确的错误信息
- 提供解决建议

---

## 📚 文档规范

### 必须维护的文档
- `README.md` - 项目说明
- `UPDATES_PAGE_SUMMARY.md` - 更新日志
- `docs/` - 功能文档

### 文档更新时机
- 添加新功能时
- 修改重要逻辑时
- 修复重大 Bug 时

---

## 🎯 开发流程

### 开发新功能
1. 在 `docs/` 创建功能文档
2. 实现功能代码
3. 添加必要的测试
4. 更新 `UPDATES_PAGE_SUMMARY.md`
5. 提交代码

### 修复 Bug
1. 定位问题原因
2. 修复代码
3. 添加防御性代码
4. 更新 `UPDATES_PAGE_SUMMARY.md`
5. 提交代码

---

## 🌐 国际化

### 多语言支持
- 所有用户可见文本使用 i18n
- 翻译文件位置：`src/i18n/locales/`
- 支持语言：中文、英文

---

## ⚡ 开发服务器

### 端口配置
- 开发环境：`3002`
- 生产环境：根据部署平台配置

### 环境变量
- 开发：`.env.local`
- 生产：部署平台配置

